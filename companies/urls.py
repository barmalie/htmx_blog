from django.urls import path
from . import views

app_name = 'companies'

urlpatterns = [
    # Здесь можно будет добавить маршруты для компаний позже,
    # например, список компаний или детальная страница.
    # Пока оставляем пустым.
]
